{
  "type": "module",
  "dependencies": {
    "@mysten/sui": "^1.18.0",
    "@types/bun": "^1.1.14",
    "bullmq": "^5.34.4",
    "dotenv": "^16.4.7",
    "yaml": "^2.6.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun index.ts",
    "build": "bun build . --target bun"
  },
  "devDependencies": {
    "@types/node": "^22.10.2"
  }
}
